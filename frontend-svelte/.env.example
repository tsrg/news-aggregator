# Public env vars (exposed to browser, must be prefixed PUBLIC_)
PUBLIC_API_BASE=http://localhost:3002
PUBLIC_REGION=Иваново

# Private env vars (server-only, Docker internal URL for backend)
API_BASE_SERVER=http://backend:3000
